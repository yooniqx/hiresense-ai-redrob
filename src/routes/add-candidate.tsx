import { createFileRoute } from '@tanstack/react-router'
import AddCandidate from '@/pages/AddCandidate'

export const Route = createFileRoute('/add-candidate')({
  component: AddCandidate,
})

// Made with Bob
